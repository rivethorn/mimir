# Mimir

Toolchain for Odin lang

**NOT EVEN BETA**

#### Current features

```bash
mimir new <project-name> # Create a new project
```

```bash
mimir build # Compile the current project
```

```bash
mimir run # Compile and run the current project
```
